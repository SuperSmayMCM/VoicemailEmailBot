name: FTPVoicemailEmailTest
dependencies:
  - ffmpeg
  - python=3.11
  - pip
  - pip:
      - msal
      - requests
      - pydub
      - openai-whisper
      - numpy<2.0  # To avoid compatibility issues with whisper